# Server Services

Use this directory for backend business logic that route handlers call.

Examples:

- `feed-ranking-service.ts`
- `creator-service.ts`
- `event-ingestion-service.ts`

Keep data fetching and validation at route boundaries; keep reusable decisions in services.
