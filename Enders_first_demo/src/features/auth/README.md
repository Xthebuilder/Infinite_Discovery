# Auth

Future home for authentication, identity, and user session state.

Near-term hackathon work:

- Keep the demo unauthenticated unless auth is essential.
- If needed, add a minimal anonymous session rather than a full login flow.
- Do not block the feed experience on auth during the hackathon.
