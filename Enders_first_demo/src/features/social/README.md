# Social

Future home for likes, comments, shares, saves, and optimistic UI mutations.

Near-term hackathon work:

- Add optimistic like/save mutations with TanStack Query.
- Add lightweight comment drawer UI.
- Keep all analytics calls behind `AnalyticsProvider`.
