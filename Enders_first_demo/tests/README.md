# Tests

Suggested hackathon testing split:

- `unit`: pure functions, schemas, stores, and ranking logic.
- `e2e`: feed navigation smoke tests and deep-link restore tests.

Add Playwright when browser-level gesture testing becomes necessary.
