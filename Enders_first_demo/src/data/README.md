# Data

Use this directory for static seed data, local fixtures, and demo content that is not runtime API logic.

Current generated feed data lives in `src/lib/feed/mock-data.ts` because route handlers import it directly.
